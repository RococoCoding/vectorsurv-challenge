Server for a simple movie form using Node / Express / SQLite3

To run the server locally:
From the root folder:
### `node server.js`

To run the client locally:
From the movie-app subfolder:
### `npm run start`